# DzrSourceHelper

CLI-утилита, которая подтягивает уровни из шаблона Google Docs, проверяет заполнение и заливает задания и файлы в движок DozoR (`classic.dzzzr.ru`).

## Что умеет
- Загрузка новых уровней или обновление существующих из структурированного Google Docs.
- Быстрая проверка документа на обязательные поля и корректные значения перед заливкой.
- Автоматическая выгрузка файлов (картинки, вложения) из Google Drive в файловый менеджер движка.
- Добавление технического уровня-заглушки после загрузки новых заданий.


# Использование (простой вариант)

**Windows**

1. При первом использовании запустите `setup.bat`
2. Отредактируйте в папке `secrets` файл `.env` со значениями:
   - LOGIN и PASSWORD — учетные данные админки DozoR.
   - CITY — код города из URL (например `moscow`).
   - GAME_ID — ID игры в админке.
   - DOCUMENT_ID — ID Google Docs с уровнями.
   - FOLDER_ID — ID папки Google Drive с файлами для уровней.
3. Положите `credentials.json` сервисного аккаунта в папку `secrets` ([инструкция по созданию](#настройка-google-cloud-для-работы-с-api-через-сервисный-аккаунт)). Сервисный аккаунт должен иметь доступ на чтение к Google Docs и к папке на Google Drive.
4. Запустите `run.bat`

**macOS и Linux**

1. В терминале из корня проекта: `chmod +x setup.sh run.sh` (достаточно один раз), затем `./setup.sh`
2. То же заполнение `secrets/.env` и размещение `secrets/credentials.json`, что и в варианте для Windows.
3. Запуск: `./run.sh` (или `./run.sh` с дополнительными аргументами, если они понадобятся `main.py`)

Нужен установленный Python 3 (на macOS обычно `python3`; при отсутствии: `brew install python`).

Доступные действия в меню:
- Добавить задания с нуля — загрузка новых уровней; при желании сразу подгружает файлы с Google Drive; после завершения добавляет технический уровень.
- Обновить существующие уровни — обновление по ID из документа.
- Закачать файлы из гугл диска в движок — только загрузка файлов без работы с заданиями.
- Выйти из программы.

Ход выполнения скрипта отображается в консоли и записывается в log/info_Y-M-d_H_m_s.log

Ошибки записываются в log/error_Y-M-d_H_m_s.log

# Использование (ручной вариант, любая ОС)

1. Установите зависимости: `pip install -r requirements.txt` (на macOS при нескольких версиях Python лучше `python3 -m pip install -r requirements.txt`).
2. Создайте папку `secrets` в корне проекта
3. Создайте в папке файл `.env` со значениями:
   - LOGIN и PASSWORD — учетные данные админки DozoR.
   - CITY — код города из URL (например `moscow`).
   - GAME_ID — ID игры в админке.
   - DOCUMENT_ID — ID Google Docs с уровнями.
   - FOLDER_ID — ID папки Google Drive с файлами для уровней.
4. Положите `credentials.json` сервисного аккаунта в папку ([инструкция по созданию](#настройка-google-cloud-для-работы-с-api-через-сервисный-аккаунт)). Сервисный аккаунт должен иметь доступ на чтение к Google Docs и к папке на Google Drive.
5. Запустите командой: `python main.py`

Доступные действия в меню:
- Добавить задания с нуля — загрузка новых уровней; при желании сразу подгружает файлы с Google Drive; после завершения добавляет технический уровень.
- Обновить существующие уровни — обновление по ID из документа.
- Закачать файлы из гугл диска в движок — только загрузка файлов без работы с заданиями.
- Выйти из программы.

Ход выполнения скрипта отображается в консоли и записывается в log/info_Y-M-d_H_m_s.log

Ошибки записываются в log/error_Y-M-d_H_m_s.log

## О формате Google Docs
- Используется структурированный документ с заголовками и таблицами. Пример: https://docs.google.com/document/d/1k6fIBklOQGYmwBHukDW25oAmH9tc5jGh71eB8KMm-Gs/edit
- Первые три вкладки в документе пропускаются, последующие вкладки считаются уровнями.
- Сквозные и бонусные настройки, тайминги подсказок, штрафы, коды, спойлеры и бонусы должны быть заполнены согласно шаблону — перед заливкой файл проходит проверку

## Полезно знать
- Файлы из Google Drive попадают в `/uploaded/{CITY}/Night/games/{GAME_ID}` на сервере.
- Файлы можно загружать только с аккаунта игротехника
- Текстовые поля преобразуются в HTML (переводы строк меняются на `<br>`, кодировка — windows-1251).
- При возникновении проблем с сетью или доступами проверьте валидность .env и права сервисного аккаунта.
  
# Настройка Google Cloud для работы с API через сервисный аккаунт

## 1. Создайте проект в Google Cloud

1.  Перейдите в [Google Cloud Console](https://console.cloud.google.com/).
2.  В верхней части страницы нажмите на выпадающий список проектов и выберите **Создать проект**.
3.  Укажите название проекта (например, `DzrSourceHelper`). Остальные поля оставьте по умолчанию и нажмите **Создать**.

## 2. Включите необходимые API

В вашем проекте должны быть включены API, которые вы планируете использовать.

1.  В левом меню перейдите в раздел **Библиотека API** (APIs & Services → Library).
2.  Найдите и включите следующие API:
    - **Google Drive API**
    - **Google Docs API**
3.  Для каждого API нажмите кнопку **Включить** (Enable).

## 3. Создайте сервисный аккаунт

Сервисный аккаунт — это учётная запись, предназначенная для автоматизированного доступа к API от имени приложения.

1.  В левом меню выберите **IAM и администрирование** → **Сервисные аккаунты**.
2.  Нажмите **Создать сервисный аккаунт**.
3.  **Шаг 1: Детали аккаунта**
    - Введите имя (например, `DzrSourceHelper`).
    - ID сформируется автоматически. Нажмите **Создать и продолжить**.
4.  **Шаг 2: Предоставить доступ к проекту (необязательно)**
    - Здесь можно назначить роли для управления проектом. Для большинства задач по чтению/записи файлов достаточно роли **Редактор** или **Просмотрщик** (в зависимости от потребностей).
    - Если вы не уверены, можете пока пропустить этот шаг, нажав **Готово**.
5.  **Шаг 3: Настройка ключа**
    - На этом шаге нужно создать ключ для аутентификации.
    - В разделе **Ключи** нажмите **Создать ключ** → выберите **JSON** → **Создать**.
    - Файл с ключом автоматически скачается на ваш компьютер. **Сохраните его в надёжном месте** (например, в папке проекта). Назовите его `credentials.json`.
6.  Нажмите **Готово**.

## 4. Предоставьте доступ сервисному аккаунту к файлам и папкам

Сервисный аккаунт имеет собственный email-адрес (например, `dzrsourcehelper@your-project.iam.gserviceaccount.com`). Чтобы он мог работать с вашими документами и папками, нужно предоставить ему доступ так же, как обычному пользователю Google Drive.

1.  Найдите email вашего сервисного аккаунта. Его можно увидеть в списке сервисных аккаунтов в консоли.
2.  Откройте Google Drive и перейдите к папке или файлу, к которым нужен доступ.
3.  Нажмите правой кнопкой мыши на объекте и выберите **Поделиться**.
4.  В поле добавления пользователя вставьте email сервисного аккаунта.
5.  Выберите уровень доступа:
    - Для чтения содержимого: **Просматриватель**.
    - Для скачивания файлов и чтения: **Просматриватель** достаточно, но для некоторых операций может потребоваться **Редактор** (например, если нужно изменять файлы).
    - Для создания и удаления файлов: **Редактор**.
6.  Нажмите **Отправить** (или **Поделиться**).

Теперь сервисный аккаунт сможет обращаться к этим ресурсам через API.  
[Вернуться назад](#использование)
